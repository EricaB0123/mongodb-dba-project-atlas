The following files are intentionally excluded from the repository:

- .env (contains Atlas connection strings and credentials)
- src/config/db.js (contains connection logic)
- src/controllers/* (contains operational logic)

To review these files, please request access.  
They are excluded to demonstrate secure operational practice and proper handling of credentials.
